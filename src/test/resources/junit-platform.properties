# Run test classes in the order defined by @Order(n) annotations on each class.
# This is for report readability (smoke first, then AC1, AC2, ...) and does not
# imply any dependency between tests; each test remains fully self-contained.
junit.jupiter.testclass.order.default=org.junit.jupiter.api.ClassOrderer$OrderAnnotation
